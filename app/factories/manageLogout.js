app.factory('manageLogout', function(){
  return{
    /*authentification = function(){
      var logkey = window.sessionStorage.getItem('snapcall-logkey');

       if (typeof logkey === 'null' || typeof logkey === 'undefined'
          || logkey === null || logkey === undefined)
          return false;
        return true;
    }*/
  }
});
